---
const { text, customClass } = Astro.props;
---

<p
  class={`${customClass} montlight tracking-[1px] text-[12px] !leading-[15px] md:text-[14px] md:!leading-[20px]
  ${text === "Back to top" && "back_to_top cursor-pointer"}
  `}
>
  {text}
</p>
